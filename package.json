{
    "name": "miniproject",
    "version": "1.0.0",
    "scripts": {
      "build": "npm run build --prefix server",
      "start": "npm run start --prefix server",
      "heroku-postbuild": "npm run build --prefix server"
    }
  }
  
  